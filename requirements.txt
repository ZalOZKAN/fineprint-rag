# Foundry Local runtime SDK (on-device inference)
foundry-local-sdk==2.0.1

# Document loading
markitdown[pdf]>=0.1.7
pymupdf>=1.26          # fallback loader for PDFs MarkItDown cannot parse

# Retrieval
numpy>=2.0

# Cross-encoder reranking. All three already ship as transitive dependencies of
# foundry-local-sdk (its runtime is onnxruntime); they are pinned here because
# rag/reranker.py now imports them directly. No torch, no transformers.
onnxruntime>=1.20
tokenizers>=0.20
huggingface_hub>=0.25

# Interface
streamlit>=1.40

# Testing
pytest>=8.0
